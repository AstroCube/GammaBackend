"use strict";

const express = require("express");
const md_auth = require("@default_auth");
const alert_controller = require("@alert_controller");

let api = express.Router();


module.exports = api;